# 🔌 Finnhub API - دليل متقدم

## الميزات الذكية 🧠

### 1. Caching ذكي
```javascript
// تحديث كل ثانيتين فقط (30 طلب/دقيقة)
// بدون التأثير على الخطة المجانية (60 طلب/دقيقة)
const FINNHUB_INTERVAL = 2000;
```

### 2. معالجة الأخطاء
```javascript
// إذا فشل الطلب → استخدم المحاكاة
// إذا انقطع الإنترنت → استمر بدونه
// بدون تأثير على الأداء
```

### 3. الحفظ الآمن
```javascript
// API Key يُحفظ في localStorage
// تشفير جانب المتصفح (آمن على الجهاز)
// لا يُرسل إلى خوادم خارجية
```

---

## الحصول على API Key

### الخطوة 1: إنشاء حساب
1. اذهب إلى [finnhub.io](https://finnhub.io)
2. اضغط "Sign Up" (مجاني)
3. أكمل التسجيل

### الخطوة 2: الحصول على الـ Key
```
Dashboard → API Keys → Copy
```

### الخطوة 3: إضافة إلى البرنامج
1. افتح التطبيق
2. اضغط على "Finnhub API"
3. ألصق الـ Key
4. اضغط "حفظ"

---

## خطة مجاني

| المعيار | الكمية |
|-------|--------|
| **الطلبات/دقيقة** | 60 |
| **الأسهم المدعومة** | جميعها |
| **البيانات** | لحظية (15+ دقيقة تأخير) |
| **الأسعار** | حقيقية |
| **الفترات الزمنية** | دقيقة |
| **البيئات** | لا حد |

---

## التحسينات المضمنة

### 📊 لا تطلب الأسعار غير الضرورية
```javascript
// فقط كل ثانيتين
if (now - lastFinnhubFetch < 2000) {
    return null; // انتظر
}
```

### ⚡ استخدام المحاكاة أثناء الانتظار
```javascript
// بينما ننتظر طلب Finnhub
// المحاكاة تستمر بدون انقطاع
sim.update();
```

### 🔄 معالجة الأخطاء التلقائية
```javascript
try {
    // حاول من Finnhub
    const data = await fetch(...);
} catch {
    // إذا فشل → استخدم المحاكاة
    sim.update();
}
```

---

## الاستهلاك الفعلي

| الحالة | الطلبات/دقيقة |
|--------|-----------------|
| **مراقبة سهم واحد** | 1 |
| **مراقبة سهمين** | 2 |
| **مراقبة 10 أسهم** | 10 |
| **الحد الأمان** | 30 |
| **الحد المجاني** | 60 |

**النتيجة:** ✅ **يمكنك مراقبة 30 سهم بأمان**

---

## مثال عملي

### بدون API (محاكاة)
```javascript
// 1 ثانية = محاكاة واقعية
// لا توجد مشاكل
// يعمل بدون إنترنت
```

### مع API (حقيقي)
```javascript
// 1 ثانية = محاكاة
// + كل ثانيتين = أسعار حقيقية
// = أفضل من الاثنين
```

---

## استكشاف الأخطاء

### ❌ "API Key غير صحيح"
- تأكد من النسخ/اللصق الصحيح
- حاول إنشاء key جديد

### ❌ "بطء في التحديثات"
- قد تكون قد وصلت حد 60 طلب
- انتظر دقيقة واحدة
- جرّب مع سهم واحد فقط

### ❌ "الأسعار غريبة"
- الأسعار صحيحة
- قد يكون هناك تأخير من Finnhub
- جرّب بدون API (محاكاة)

---

## الخلاصة

✅ **API Key اختياري 100%**
✅ **البرنامج يعمل بدونه**
✅ **المحاكاة واقعية جداً**
✅ **لا حدود على الاستخدام**

---

**جرّب بدون API أولاً، ثم أضفه إذا أردت أسعار حقيقية!** 🚀
